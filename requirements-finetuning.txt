# Minimal requirements for fine-tuning on macOS (Apple Silicon compatible)
# Install with: pip install -r requirements-finetuning.txt

# Core dependencies (needed for R2 download)
python-dotenv>=1.0.0
pydantic>=2.0.0
boto3>=1.28.0

# PyTorch (macOS version with MPS support)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Transformers and fine-tuning - PINNED FOR MACOS COMPATIBILITY
transformers==4.40.2
datasets>=2.14.0
accelerate==0.28.0
peft==0.11.1
trl<0.9.0

# System utilities
psutil>=5.9.0
tqdm>=4.65.0
numpy>=1.24.0,<2.0.0

# Google API (for data collection)
google-api-python-client>=2.0.0
google-auth-oauthlib>=0.5.0
google-auth-httplib2>=0.1.0
